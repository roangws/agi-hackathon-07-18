/* Copy this file to insforge-config.js and fill in your InsForge project values.
   Find them with:  npx @insforge/cli current --json   (project.oss_host + project.api_key)
   insforge-config.js is gitignored so your key never lands in the repo. */
window.INSFORGE = {
  host: "https://<appkey>.<region>.insforge.app",
  key:  "ik_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  table: "mesh_events",
};
